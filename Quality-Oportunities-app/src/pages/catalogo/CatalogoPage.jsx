export default function CatalogoPage({ onSelectReto }) {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center">
      <p className="text-on-surface-variant">Catálogo de retos (próximamente)</p>
    </div>
  );
}
