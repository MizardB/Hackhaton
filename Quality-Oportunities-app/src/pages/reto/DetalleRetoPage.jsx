export default function DetalleRetoPage({ onIniciar }) {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center">
      <p className="text-on-surface-variant">Detalle del reto (próximamente)</p>
    </div>
  );
}
